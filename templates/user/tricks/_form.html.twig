{{ form_start(form) }}
{{ form_rest(form) }}
<img src="/uploads/files/{{ tricks.thumbnail }}" style="width: 100px; height: auto; margin-bottom: 2%;"> <br>
<button class="btn btn-primary">{{ button|default('Save') }}</button>
{{ form_end(form) }}